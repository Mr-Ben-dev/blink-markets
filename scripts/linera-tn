#!/usr/bin/env bash
set -euo pipefail
root="$(pwd)"
dir="$root/.linera/testnet"
mkdir -p "$dir"
export LINERA_WALLET="$dir/wallet.json"
export LINERA_KEYSTORE="$dir/keystore.json"
export LINERA_STORAGE="rocksdb:$dir/wallet.db"
exec linera "$@"
